# 交接文档 — 新对话从这里开始

> 本文件是给**新对话**的手持交接:当前状态、未完成事项、确切命令、关键文件。
> 生成日期:2026-08-31。读完本文件 + 两份方案 txt,即可无缝续接。

---

## 0. 项目是什么(30 秒版)

**LLM 引导的闭环元层进化框架**,自动设计 HFSP(混合流水车间, makespan)元启发式算法。

- **创新②(防塌缩, 主攻)**:strategy collapse 现象 + 多样性保持进化 + 洞察 Archive
- **创新①(自进化, 后续)**:进化数据 DPO→RLVR 偏好对齐(阶段6, 已延期)
- **载体**:HFSP 480 基准(可验证奖励 makespan)

**权威方案**:`第一篇论文_改动实施清单.txt` + `LLM自进化启发式算法_系统整体设计骨架.txt`(项目根目录)+ `docs/方案设计.md`。

---

## 1. 已完成(当前状态)

### 框架与实验
- 组件空间(AlgorithmSpec) + 执行器 + LLM 进化 + 反思 + 并行
- 正式划分:40 训练子集(尺寸到 160)、144 测试(黑盒)、K 折支持
- **主结果**:自动设计的算法测试集 RPD ≈ 5.96-6.00% < IG 6.12%(三个 run 一致, 赢 IG)
- **关键发现**:LLM 搜索 ≈ 随机搜索(测试集打平)→ 论文不靠"LLM 搜索更强",靠"防塌缩 + 自进化"
- **塌缩已复现**:init spec 赢 10 代 best 平线 → 验证了方案的核心诊断

### 大改代码(阶段 1-5, 全部实现并冒烟通过)
| 阶段 | 内容 | 文件 |
|---|---|---|
| 1 | 多 seed + 喂种群排名 | `make_fitness(n_runs)`, `evolve.py` rank 反馈, `prompts.py` 显示排名 |
| 2 | 组件池扩容 | `operators/block.py`, `heuristics/palmer.py`, `heuristics/cds.py`, `record_to_record` 接受, `components.py` |
| 3 | 轻/中变异 + 交叉(60/30/10) | `evolve.py` mutation_ratios, `mutator.py` crossover/heavy |
| 4 | 多样性惩罚 + 追踪 | `diversity.py`, `evolve.py` diversity_lambda + history_diversity |
| 5 | Archive 洞察 + 重变异 | `archive.py`, `prompts.py` insight/heavy prompt |

**运行主脚本**:`scripts/run_evolution_full.py`(参数: `--gen 10 --rho 0.5 --jobs 6 --n-runs 3 --diversity 0.05`)

---

## 2. 未完成(按优先级)

### 🔴 证据 2(下一仗, 最优先):防塌缩版进化验证
- **目的**:用上所有新机制(多seed/多样性/Archive/交叉),验证**进化曲线真的往下走、种群不塌缩**
- **做法**:跑 `run_evolution_full.py --diversity 0.05 --n-runs 5 --gen 15`,小规模(或 40 子集)
- **验收**:best RPD 单调/阶梯下降(不是平线);`evolution_curve.csv` 的 diversity 列不塌到 1/8
- **产出**:证据 2 的图(进化曲线 + 多样性曲线)

### 🟠 阶段 6(延期, 用户确认放后面):DPO 自训练闭环(创新①)
- 用进化攒偏好对(同父代好/坏变异)→ LoRA 微调 qwen → 三层对照(微调后>微调前>零样本)
- 需要:租 GPU、攒几千偏好对、LoRA 训练
- 详细方案:见 `第一篇论文_改动实施清单.txt` 改动 6

### 🟡 证据 1:SMAC/irace 同预算对比
- 同一组件空间 + 相同总评估预算 → 预算-质量曲线
- 卖点:"LLM 用先验换样本效率"

### 🟡 证据 4:标准 benchmark(CO-Bench / LLM4AD 至少一个)
- 需要调研这两个 benchmark 的格式/接入

### 🟢 论文写作
- 创新②(防塌缩) + 创新①(自进化) 两篇规划(见骨架文档 §九)
- 目标期刊:ASOC/EAAI(第一篇)、SWEVO/TEVC(第二篇);热身 GECCO/CEC

---

## 3. 确切命令(续接用)

```bash
cd /Users/soda/Desktop/研究生学习工作/hfsp
# 防塌缩版进化(证据2 初验)
.venv/bin/python -u scripts/run_evolution_full.py --gen 15 --pop 8 --rho 0.5 --jobs 6 --n-runs 5 --diversity 0.05
# 随机对照(同设置)
.venv/bin/python -u scripts/run_ablation_random.py --gen 15 --pop 8 --rho 0.5 --jobs 6 --n-runs 5
# 测试评估(进化完, 合并基线)
.venv/bin/python -u scripts/eval_spec_test.py --spec results/llm/base_run/best_spec.json --label g1_final --rho 2
```

**环境**:`.venv`(Python 3.14, 已装 numpy/pandas/scipy/ollama 相关)。Ollama 本地 qwen2.5:14b。

---

## 4. 关键文件地图

```
docs/方案设计.md           ← 论文总纲(大改后: 塌缩+双创新+四证据)
docs/idea_roadmap.md       ← 想法路线图(A-H)
docs/p0_literature_review.md ← 文献综述(SOTA/谱系)
第一篇论文_改动实施清单.txt  ← 7 阶段实施计划(权威)
LLM自进化启发式算法_系统整体设计骨架.txt ← 系统设计(权威)
hfsp/llm/                  ← 核心代码(executor/evolve/mutator/reflection/archive/diversity/dataset)
hfsp/methods/operators/block.py, heuristics/palmer.py cds.py  ← 阶段2 新增
results/llm/               ← 所有实验结果(base_run/p5d/ablation_random/test_evals...)
```

---

## 5. 给新对话的三句话

1. 系统已完成**阶段 1-5**(多seed/组件扩容/变异交叉/多样性/Archive),**证据 2 还没验**——先跑防塌缩版进化,看曲线是否真下降。
2. **DPO(阶段6)用户明确延期**,别主动开始;创新①靠它,但先做证据 2。
3. 论文核心主张已定:**防塌缩(创新②)+ 自进化(创新①)**,不靠"LLM 搜索更强"(已证伪)。
